Restarted the provisioning agent service

Restarted the agent updater service

Restarted provisioning from the Entra portal

Disabled and re-enabled verbose logging

Uninstalled and reinstalled the provisioning agent

Re-registered the provisioning agent with Microsoft Entra ID

Reconnected the on-premises Active Directory domain

Confirmed the gMSA service account is enabled

Confirmed the Microsoft.Azure.SyncFabric service principal is enabled

Confirmed the on-premises directory synchronization service account is enabled

Confirmed the Cloud Sync job schedule is active

Confirmed outbound TCP 443 connectivity

Confirmed LDAP connectivity to the domain controller

Confirmed PowerShell execution policy is RemoteSigned

Confirmed no WinHTTP proxy is configured

Confirmed no TLS inspection certificate is being presented

Confirmed the agent process is running and responsive
